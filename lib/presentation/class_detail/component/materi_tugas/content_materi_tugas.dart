import 'package:flutter/material.dart';

class ContentMateriTugas extends StatelessWidget {
  const ContentMateriTugas({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Materi & Tugas Content"));
  }
}