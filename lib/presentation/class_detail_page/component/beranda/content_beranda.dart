import 'package:flutter/material.dart';

class ContentBeranda extends StatelessWidget {
  const ContentBeranda({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Beranda Content"));
  }
}